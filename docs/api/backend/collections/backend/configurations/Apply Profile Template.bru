meta {
  name: Apply Profile Template
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/configurations/tenants/{{tenantId}}/apply
  body: json
  auth: inherit
}

body:json {
  {
    "templateId": "{{templateId}}"
  }
}

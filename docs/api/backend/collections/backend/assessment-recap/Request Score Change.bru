meta {
  name: Request Score Change
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/assessment-recap/change-requests
  body: json
  auth: inherit
}

body:json {
  {
    "classSubjectId": "{{classSubjectId}}",
    "periodId": "{{academicPeriodId}}"
  }
}
